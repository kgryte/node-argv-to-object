'use strict';

// PARSE BOOLEAN //

/**
* FUNCTION: parseBoolean( val )
*	Coerces an input value to a boolean.
*
* @param {Boolean} value - value to coerce
* @returns {Boolean|Error} boolean or error object
*/
function parseBoolean( val ) {
	return val;
} // end FUNCTION parseBoolean()


// EXPORTS //

module.exports = parseBoolean;
